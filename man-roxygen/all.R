#' @param key A CAX API token. See \code{\link{rcax_key}}.
#' @param ... Curl options passed to \code{\link[crul]{HttpClient}}
#' @param parse (logical) Whether to parse to list (\code{FALSE}) or data.frame (\code{TRUE}). Default: \code{TRUE}
